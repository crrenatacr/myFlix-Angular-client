import { UserRegistrationFormComponent } from './user-registration-form.component'

describe('UserRegistrationFormComponent', () => {
  it('should mount', () => {
    cy.mount(UserRegistrationFormComponent)
  })
})